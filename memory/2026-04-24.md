# 2026-04-24

- Aligned app runtime with platform guidance by migrating server from raw Node HTTP to Express.
- Updated `package.json` to include `express` dependency.
- Rewrote `server.js` to use `express.static`, explicit root route, and `app.listen` on `0.0.0.0` + `PORT`.
- Deployed and verified production endpoint returned HTTP 200.
- Added CSP header middleware in `server.js` before static middleware.
- Observed edge response still returns an empty `content-security-policy` header value, indicating upstream override.
- Removed the ineffective app-level CSP middleware and documented the platform limitation in `TOOLS.md`.
- Added final Express 404 middleware to return `index.html` for unmatched routes with `404` status.
- External probe for `/does-not-exist` still returned HTTP 200, indicating platform/edge route handling may override app-level 404 status.
